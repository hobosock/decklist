/// simple card format for collections and decklists
/// just the card name and the quantity
pub struct CollectionCard {
    pub name: String,
    pub quantity: u64,
}

// TODO: read in collection files
